import 'package:flutter/material.dart';

class TreatCard extends StatelessWidget {
  const TreatCard({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("data"));
  }
}
